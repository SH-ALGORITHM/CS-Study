# 측정 기록

자동 누적. 옆에 해석 메모는 직접 추가하세요.

- [05-26 03:29] s1 · Bean 라이프사이클: 싱글톤은 close 시 @PreDestroy 호출되나, 프로토타입은 호출 안 됨 확인
1. 싱글톤은 "미리" 만들어진다 (Eager Initialization)
   --- [1] 싱글톤 빈 관찰 --- 로그가 찍히기도 전에 NormalBean의 생성자가 호출된 거 보이시죠?
* 의미: 스프링 컨테이너는 부팅될 때 특별한 설정이 없다면 모든 싱글톤 빈을 미리 다 만들어 둡니다. 그래서 실제 쓸 때는
  이미 준비된 객체를 바로 꺼내 쓰기만 하면 돼요.

2. 프로토타입은 "요청할 때" 만들어진다 (Lazy)
   ProtoBean 첫 번째 요청: 로그 이후에 생성자 로그가 찍혔죠?
* 의미: 프로토타입 빈은 스프링이 미리 안 만들어둡니다. 누군가 "꺼내 줘!(getBean)"라고 할 때만 새로 생성해서
  던져줍니다.

3. 프로토타입의 소멸(@PreDestroy)은 스프링 책임이 아니다
   ctx.close()를 했을 때 NormalBean은 인사를 했지만, ProtoBean은 침묵했습니다.
* 의미: 스프링 컨테이너는 프로토타입 빈을 "생성하고 주입해 주는 것"까지만 책임집니다. 그 이후에 이 객체를 버릴지
  말지는 꺼내 간 사람(클라이언트 코드)의 책임이에요. 그래서 스프링이 죽을 때 프로토타입 빈의 소멸 메서드를 대신
  호출해 주지 않습니다.
- [05-26 03:39] s1 · Bean 라이프사이클 및 개수: 싱글톤 소멸 확인, 프로토타입 미소멸 확인, 전체 빈 9개 확인
  🔍 잠깐, 이 일꾼들은 누구일까요? (중요!)
  리스트를 자세히 보면 아주 흥미로운 이름들이 있습니다:
  1. internalAutowiredAnnotationProcessor: 우리가 코드에 @Autowired를 붙였을 때, 그게 누군지 찾아내서 실제로 꽂아주는
     역할을 하는 빈입니다. (얘가 없으면 @Autowired는 작동 안 해요!)
  2. internalCommonAnnotationProcessor: @PostConstruct, @PreDestroy 같은 표준 어노테이션을 인식해서 오늘 우리가 본
     로그를 찍게 해주는 빈입니다.
- [05-26 03:49] s1 · Scan vs Bean & DI 관찰: 자동 스캔과 수동 등록의 차이, @Autowired를 통한 주입 확인
  1. @Component vs @Bean: @Component는 내 코드, @Bean은 외부 라이브러리(수정 불가) 객체 등록에 사용.
  2. 생성자 주입: 생성자가 하나일 땐 @Autowired 생략 가능(스프링 4.3+). 객체를 new로 직접 조립하지 않아도 스프링이 의존성을 꽂아줌.
- [05-26 04:00] s1 · Spring Boot 빈 개수 측정: 전체 빈 283개 확인. @SpringBootApplication이 가져오는 자동 설정(Tomcat, JPA, DB Pool 등)의 규모를 체감함. devtools에 의한 재시작으로 로그가 중복 출력됨을 확인.
- [05-26 04:15] s2-1 · 3주차 코드 마이그레이션 (Before vs After): 직접 팩토리 관리 방식에서 스프링 @Configuration & @Bean 방식으로 전환 완료
  1. 객체 생성 및 소멸 책임 전가: Before에서는 직접 close()를 호출했으나, After에서는 ctx.close()가 @Bean(destroyMethod="close")를 통해 자동으로 자원을 정리함.
  2. 자동 의존성 주입 확인: AuthRepository 생성자에 별도의 조립 코드 없이도 DataSource가 정상적으로 주입됨(repository injected DataSource type 확인).
  3. 설정 분리: DataSourceConfig, RedisConfig로 설정을 분리하고 @Import를 통해 관리하는 구조로 개선.
- [05-29 04:47] s2-2 · 계층 분리 및 다형성 주입 확인: AuthService와 AuthRepository 연동 및 @Qualifier 기반 주입
  1. 생명주기 순서 관찰: 모든 빈의 생성자 호출 -> @PostConstruct 호출 -> 실제 비즈니스 로직(login) -> context 종료 시 @PreDestroy 호출 순서로 진행됨을 명확히 확인.
  2. 다형성(Strategy) 동작: AuthService는 AuthProvider 인터페이스에만 의존하지만, @Qualifier("jwt")에 의해 런타임에 JwtAuthProvider가 주입되어 동작함.
  3. 자동 자원 관리: context.close() 시점에 개발자의 명시적 호출 없이도 HikariCP 커넥션 풀이 안전하게 Shutdown 됨.
- [05-29 04:55] s2-3 · 주입 방식 3가지 비교 (생성자/필드/세터)
  1. 생성자 주입: 객체가 태어나는 순간 의존성이 완벽하게 조립됨을 확인 (`null 아님? true`). 가장 안전하며 불변성(final)을 보장.
  2. 필드 및 세터 주입: 객체가 빈 껍데기로 먼저 태어난 후 나중에 주입됨 (`아직 주입 안됨 (null? true)`). 런타임에 의존성이 바뀔 위험성이 있음.
- [05-29 05:23] s3-1 · 순수 main(): 부팅 시간 5ms / 객체 6 개
- [05-29 05:23] s3-1 · AnnotationConfigContext: 부팅 시간 1088ms / Bean 15개
- [05-29 05:23] s3-1 · SpringApplication.run(): 부팅 시간 6123ms / Bean 282개
  - **결과 해석 (STAGE 3-1):** 
    1. **순수 Java (5ms):** 번개처럼 빠르지만, 모든 의존성을 개발자가 직접 `new`로 엮어줘야 해서 유지보수가 불가능에 가깝다.
    2. **순수 Spring (1088ms / 15개):** 컴포넌트 스캔을 돌고 빈을 생성/조립하는 과정 때문에 순수 Java보다 약 200배 느리다. (우리가 만든 빈과 스프링 기본 빈 포함 15개)
    3. **Spring Boot (6123ms / 282개):** 가장 느리다. 하지만 282개의 빈 속에는 내장 톰캣 서버, JPA 설정, HikariCP 커넥션 풀 등 웹 애플리케이션에 필요한 모든 인프라가 자동으로 셋팅되어 있다. **"초기 구동 속도를 지불하고, 엄청난 편리함과 확장성을 샀다"**고 볼 수 있다.
- [05-29 05:23] s3-3 · 싱글톤 1000회 getBean: 생성자 호출 1회
- [05-29 05:23] s3-3 · 프로토타입 1000회 getBean: 생성자 호출 1000회
  - **결과 해석 (STAGE 3-3):** 
    - **싱글톤:** 1000번을 불러도 최초 1회만 객체를 생성하여 재사용함. 메모리 관리에 극도로 효율적이나 내부에 상태(Stateless)를 가지면 안 됨.
    - **프로토타입:** 매 요청마다 `new` 연산을 수행하여 1000개의 객체가 메모리에 생성됨. GC(가비지 컬렉터)에 큰 부담을 주므로 특별한 이유가 없다면 사용을 피해야 함.
- [05-29 05:23] s3-4 · Eager (@Lazy 없음): 부팅 시간 2913ms
- [05-29 05:23] s3-4 · Lazy (@Lazy 적용): 부팅 40ms / 첫 호출 2022ms
  - **결과 해석 (STAGE 3-4):** 
    - **Eager:** 객체 생성 비용(2초)이 서버 부팅 시간에 모두 포함되어 서버 기동이 느려짐(2913ms).
    - **Lazy:** 무거운 객체 생성을 뒤로 미뤄서 서버 부팅은 매우 빠름(40ms). 단, 첫 번째 접속자가 이 객체를 사용할 때 생성 지연(2022ms)을 고스란히 떠안게 되는 큰 단점이 존재함. (UX 저하)
- [05-29 05:01] s2-4 · 다형성 Map 자동 주입: AuthProvider 인터페이스를 구현한 모든 빈을 Map<String, AuthProvider>로 주입받아 OCP(개방폐쇄원칙) 만족 확인
- [05-29 05:16] s2-4 · @Qualifier 직접 조회: 빈 이름을 명시하여 ctx.getBean("이름", 타입)으로 4개의 다형성 객체를 독립적으로 조회 확인
- [05-29 05:17] s2-4 · 다형성 Map 자동 주입: AuthProvider 인터페이스를 구현한 모든 빈을 Map<String, AuthProvider>로 주입받아 OCP(개방폐쇄원칙) 만족 확인
- [05-29 05:23] s3-1 · 순수 main(): 부팅 시간 5ms / 객체 6 개
- [05-29 05:23] s3-1 · AnnotationConfigContext: 부팅 시간 1088ms / Bean 15개
- [05-29 05:23] s3-1 · SpringApplication.run(): 부팅 시간 6123ms / Bean 282개
- [05-29 05:28] s3-3 · 싱글톤 1000회 getBean: 생성자 호출 1회
- [05-29 05:28] s3-3 · 프로토타입 1000회 getBean: 생성자 호출 1000회
- [05-29 05:28] s3-4 · Eager (@Lazy 없음): 부팅 시간 2913ms
- [05-29 05:28] s3-4 · Lazy (@Lazy 적용): 부팅 40ms / 첫 호출 2022ms
- [05-29 05:33] s4-1 · 순환 참조 시도 3가지: 부팅 실패 2건 / 통과 1건
  - **결과 해석 (STAGE 4):**
    1. **생성자 주입 순환 (실패):** A를 만들려면 B가 필요하고, B를 만들려면 A가 필요한 "닭과 달걀" 상황. 시작조차 못 하고 즉시 에러(`BeanCurrentlyInCreationException`)를 뿜으며 장렬히 전사한다. (가장 좋은 에러! 배포 전에 문제를 알 수 있음)
    2. **필드 주입 과거 버전 (통과):** 일단 빈 껍데기만 먼저 만들어놓고 통과시킨 뒤, 나중에 주사기로 서로를 찔러 넣어준다. 하지만 이 방식은 런타임에 덜 초기화된 객체를 호출하다가 대형 장애(NPE)를 일으킬 위험이 있다.
    3. **필드 주입 최신 버전 (실패):** 스프링 부트 2.6부터는 위와 같은 런타임 장애를 원천 차단하기 위해 필드 주입의 순환 참조조차 부팅 시점에 칼같이 막아버린다. (`allowCircularReferences = false`가 기본값)
- [05-29 05:40] s4-3 · @Lazy로 순환참조 회피: 성공 - 프록시(가짜) 객체를 대신 주입받아 통과함
- [05-29 05:40] s4-3 · 설계 변경으로 근본 해결: 성공 - 공통 기능을 분리하여 A와 B의 양방향 의존을 완벽히 끊어냄
  - **해결책 해석:**
    - `@Lazy`를 쓰면 스프링이 진짜 빈 대신 `CGLIB Proxy`(가짜 껍데기)를 먼저 주입해주어 부팅 에러를 막아준다. 하지만 양방향 의존성이라는 나쁜 설계 자체는 그대로 남아있는 임시방편이다.
    - 결국 A와 B가 공통으로 필요로 하는 기능을 `CommonService` 등 제3의 클래스로 빼내어 단방향 의존성으로 **설계를 뜯어고치는 것**이 유일한 정답이다.
- [05-29 05:33] s4-1 · 순환 참조 시도 3가지: 부팅 실패 2건 / 통과 1건
- [05-29 05:36] s4-3 · @Lazy로 순환참조 회피: 성공 - 프록시(가짜) 객체를 대신 주입받아 통과함
- [05-29 05:36] s4-3 · 설계 변경으로 근본 해결: 성공 - 공통 기능을 분리하여 A와 B의 양방향 의존을 완벽히 끊어냄
